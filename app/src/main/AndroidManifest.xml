<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <queries>
        <!-- 런처가 있는 앱들 전체 조회 가능 -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
    <application
            android:name=".PayhereEntryApplication"
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Payhere_Entry"
            tools:targetApi="31">
        <activity
                android:name=".ui.MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:launchMode="singleTask"
                android:theme="@style/Theme.Payhere_Entry">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <provider
                android:name=".provider.SelectedAppProvider"
                android:authorities="com.teclast_korea.payhere_entry.provider"
                android:exported="true"
        />
        <!-- The broadcast receiver is exported so external apps can trigger it -->
        <receiver
                android:name=".receiver.ResetReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="ACTION_RESET" />
                <action android:name="ACTION_SET_HOME_APP" />
                <action android:name="ACTION_TURN_OFF" />
            </intent-filter>
        </receiver>
    </application>

</manifest>